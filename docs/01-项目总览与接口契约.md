# 项目总览与接口契约

**文档版本**: v1.0
**更新日期**: 2026-03-21
**用途**: 多 Agent 并行开发的共享参考文档，定义所有跨模块接口、事件、DI 注册规范

---

## 一、项目当前状态

### 已完成（Phase 1 + Phase 2）

| 模块 | 说明 | 关键文件 |
|------|------|---------|
| 设备连接 | 串口/TCP 连接，状态监控 | `Core/Communication/SerialDeviceConnection.cs`, `TcpDeviceConnection.cs` |
| 协议解析 | ASCII/Binary 协议，帧段式配置 | `Core/Protocol/AsciiLineParser.cs`, `ConfigurableBinaryParser.cs` |
| 传感器适配 | 四种传感器类型适配器 | `Core/Sensors/` 目录 |
| 实时图表 | ScottPlot 多通道绘制，多图表模式 | `App/ViewModels/RealtimeChartViewModel.cs` |
| 计算通道 | Shunting-yard 公式引擎，总场/梯度/自定义向导 | `Core/Helpers/FormulaEvaluator.cs` |
| 通道偏移 | 显示偏移 + 自动偏移 | `Core/Models/ChannelDisplayConfig.cs` |
| 协议管理 | 导入/导出/内置预设 | `App/ViewModels/ConnectionViewModel.cs` |
| DataBus | 发布-订阅解耦 | `Core/Services/DataBus.cs` |

### 未完成

| 阶段 | 任务 | 状态 |
|------|------|------|
| Phase 2 遗留 | B-3 LTTB 降采样、B-7 滚动统计 UI 集成 | 未开始 |
| Phase 3 | D-1~D-4 数据存储/会话/回放/导出 | 未开始 |
| Phase 4 | C-1~C-5 正交度校正、E-1~E-3 数据处理 | 未开始 |
| Phase 5 | F-2 错误处理、F-3 配置持久化 | 未开始 |

### 空目录（有结构无代码）

```
Core/Calibration/       ← 正交度校正算法
Core/Processing/        ← 滤波、统计处理
Infrastructure/Database/ ← SQLite 存储
Infrastructure/Export/   ← CSV 导出
Core.Tests/             ← 0 个测试文件
```

---

## 二、系统架构

```
┌───────────────────────────────────────────────────┐
│                    WPF App Layer                   │
│  ┌─────────────┐ ┌──────────────┐ ┌────────────┐ │
│  │ Connection   │ │ RealtimeChart│ │ History    │ │
│  │ ViewModel    │ │ ViewModel    │ │ Playback   │ │
│  └──────┬───────┘ └──────┬───────┘ └─────┬──────┘ │
│         │                │               │        │
│  ┌──────┴────────────────┴───────────────┴──────┐ │
│  │                  DataBus (pub/sub)            │ │
│  └──────┬────────────────┬───────────────┬──────┘ │
└─────────┼────────────────┼───────────────┼────────┘
          │                │               │
┌─────────┴──────┐ ┌──────┴───────┐ ┌─────┴────────┐
│  Core Layer    │ │ Infrastructure│ │  Calibration │
│  - Protocol    │ │ - SQLite     │ │  - Ortho     │
│  - Sensors     │ │ - CSV Export │ │  - HardIron  │
│  - Processing  │ │ - Config     │ │  - SoftIron  │
│  - Helpers     │ │              │ │              │
└────────────────┘ └──────────────┘ └──────────────┘
```

---

## 三、共享接口契约

### 3.1 DataBus 事件（`Core/Services/DataBus.cs`）

**现有事件（勿修改签名）：**

```csharp
namespace MagnetometerSystem.Core.Services;

public class DataBus
{
    // --- 现有 ---
    event Action<MagnetometerReading>? ReadingReceived;
    event Action<SensorConfig>? AcquisitionStarted;
    event Action? AcquisitionStopped;

    // --- 新增（TASK-D1 实现）---
    event Action<string>? SessionStarted;    // 参数: sessionId
    event Action<string>? SessionEnded;      // 参数: sessionId
}
```

**发布规则：**
- `ReadingReceived`: 由 `ConnectionViewModel.OnDataReceived` 在后台线程发布
- `AcquisitionStarted/Stopped`: 由 `ConnectionViewModel` 在 UI 线程发布
- `SessionStarted/Ended`: 由存储服务在会话创建/结束时发布

### 3.2 IDataStorageService（`Core/Storage/IDataStorageService.cs`）

**已定义，状态：待实现。实现文件：`Infrastructure/Database/SqliteStorageService.cs`**

```csharp
namespace MagnetometerSystem.Core.Storage;

public interface IDataStorageService
{
    Task<string> StartSessionAsync(string name, SensorConfig sensorConfig, ConnectionConfig connectionConfig);
    Task EndSessionAsync(string sessionId);
    Task SaveReadingsAsync(IEnumerable<MagnetometerReading> readings);
    Task<IReadOnlyList<SessionInfo>> GetSessionsAsync();
    Task<IReadOnlyList<MagnetometerReading>> GetReadingsAsync(
        string sessionId, DateTime? startTime = null, DateTime? endTime = null);
    Task DeleteSessionAsync(string sessionId);
}

public class SessionInfo
{
    public string Id { get; set; } = string.Empty;
    public string Name { get; set; } = string.Empty;
    public DateTime StartedAt { get; set; }
    public DateTime? EndedAt { get; set; }
    public SensorType SensorType { get; set; }
    public double SampleRate { get; set; }
    public int ChannelCount { get; set; }
    public string[] ChannelNames { get; set; } = [];
    public string? DeviceInfo { get; set; }
    public ConnectionType ConnectionType { get; set; }
    public string? Notes { get; set; }
    public long TotalReadings { get; set; }
}
```

### 3.3 ICalibrationRepository（新增，`Core/Calibration/ICalibrationRepository.cs`）

**供 TASK-C5 实现，TASK-C3 消费：**

```csharp
namespace MagnetometerSystem.Core.Calibration;

public interface ICalibrationRepository
{
    Task SaveOrthogonalityProfileAsync(OrthogonalityParams profile);
    Task<IReadOnlyList<OrthogonalityParams>> GetOrthogonalityProfilesAsync(string? sensorSerial = null);
    Task<OrthogonalityParams?> GetOrthogonalityProfileAsync(string id);
    Task DeleteOrthogonalityProfileAsync(string id);

    Task SaveCalibrationProfileAsync(CalibrationParams profile);
    Task<IReadOnlyList<CalibrationParams>> GetCalibrationProfilesAsync(SensorType? sensorType = null);
    Task DeleteCalibrationProfileAsync(string id);
}
```

### 3.4 IOrthogonalityService（新增，`Core/Calibration/IOrthogonalityService.cs`）

```csharp
namespace MagnetometerSystem.Core.Calibration;

/// <summary>
/// 正交度计算与校正服务
/// </summary>
public interface IOrthogonalityService
{
    /// <summary>
    /// 从全方位旋转采集数据计算正交度补偿矩阵
    /// </summary>
    /// <param name="rawData">N x 3 矩阵（每行为 Bx, By, Bz）</param>
    /// <param name="referenceFieldStrength">参考场强(nT)，null 则自动估计</param>
    OrthogonalityResult Calculate(double[,] rawData, double? referenceFieldStrength = null);

    /// <summary>
    /// 对单个三轴读数应用正交度校正
    /// </summary>
    double[] Apply(OrthogonalityParams parameters, double x, double y, double z);

    /// <summary>
    /// 评估拟合质量
    /// </summary>
    FitQuality EvaluateFit(OrthogonalityParams parameters, double[,] rawData);
}

/// <summary>正交度计算结果</summary>
public class OrthogonalityResult
{
    public OrthogonalityParams Parameters { get; set; } = new();
    public FitQuality Quality { get; set; } = new();
    public bool Success { get; set; }
    public string? ErrorMessage { get; set; }
}

/// <summary>拟合质量指标</summary>
public class FitQuality
{
    public double ResidualMean { get; set; }
    public double ResidualStd { get; set; }
    public double MaxResidual { get; set; }
    public int SampleCount { get; set; }
    public double SphericityCoverage { get; set; }  // 方位角覆盖度 0~1
}
```

### 3.5 ICalibrationService（新增，`Core/Calibration/ICalibrationService.cs`）

```csharp
namespace MagnetometerSystem.Core.Calibration;

/// <summary>
/// 通用传感器校准服务（硬铁/软铁）
/// </summary>
public interface ICalibrationService
{
    /// <summary>从静态采集数据计算偏移量</summary>
    CalibrationParams CalculateOffset(double[,] staticData, int channelCount);

    /// <summary>从参考数据计算增益</summary>
    CalibrationParams CalculateGain(double[,] referenceData, double[] knownValues);

    /// <summary>对原始值应用校准</summary>
    double[] Apply(CalibrationParams parameters, double[] rawValues);
}
```

### 3.6 IDataProcessor（新增，`Core/Processing/IDataProcessor.cs`）

```csharp
namespace MagnetometerSystem.Core.Processing;

/// <summary>
/// 数据处理接口（滤波、统计）
/// </summary>
public interface IDataProcessor
{
    /// <summary>移动平均滤波</summary>
    double[] MovingAverage(double[] data, int windowSize);

    /// <summary>中值滤波</summary>
    double[] MedianFilter(double[] data, int windowSize);
}
```

### 3.7 IDataExporter（新增，`Infrastructure/Export/IDataExporter.cs`）

```csharp
namespace MagnetometerSystem.Infrastructure.Export;

/// <summary>数据导出接口</summary>
public interface IDataExporter
{
    string Format { get; }  // "CSV", "Binary" 等
    Task ExportAsync(string sessionId, string filePath, ExportOptions options,
        IProgress<double>? progress = null, CancellationToken ct = default);
}

public class ExportOptions
{
    public int[]? ChannelIndices { get; set; }       // null = 全部通道
    public DateTime? StartTime { get; set; }
    public DateTime? EndTime { get; set; }
    public bool IncludeCalibratedData { get; set; }
    public bool IncludeHeader { get; set; } = true;
}
```

### 3.8 IAppConfigService（新增，`Infrastructure/Configuration/IAppConfigService.cs`）

```csharp
namespace MagnetometerSystem.Infrastructure.Configuration;

public interface IAppConfigService
{
    Task<T?> GetAsync<T>(string key);
    Task SetAsync<T>(string key, T value);
    Task<AppSettings> LoadSettingsAsync();
    Task SaveSettingsAsync(AppSettings settings);
}

public class AppSettings
{
    public string? DefaultPortName { get; set; }
    public int DefaultBaudRate { get; set; } = 115200;
    public string? DefaultIpAddress { get; set; }
    public int DefaultPort { get; set; } = 5000;
    public string DataStoragePath { get; set; } = "";
    public bool AutoSaveEnabled { get; set; } = true;
    public int ChartRefreshRate { get; set; } = 30;
    public string ThemeName { get; set; } = "Default";
}
```

---

## 四、现有模型参考表

| 模型 | 文件路径 | 状态 | 可否修改 |
|------|---------|------|---------|
| `MagnetometerReading` | `Core/Models/MagnetometerReading.cs` | 完成 | 仅追加属性 |
| `SensorConfig` | `Core/Models/SensorConfig.cs` | 完成 | 仅追加属性 |
| `SensorType` | `Core/Models/SensorType.cs` | 完成 | 禁止修改 |
| `ConnectionConfig` | `Core/Models/ConnectionConfig.cs` | 完成 | 禁止修改 |
| `ProtocolConfig` | `Core/Models/ProtocolConfig.cs` | 完成 | 禁止修改 |
| `FrameSegment` | `Core/Models/FrameSegment.cs` | 完成 | 禁止修改 |
| `OrthogonalityParams` | `Core/Models/OrthogonalityParams.cs` | 完成 | 仅追加属性 |
| `CalibrationParams` | `Core/Models/CalibrationParams.cs` | 完成 | 仅追加属性 |
| `ChartSettings` | `Core/Models/ChartSettings.cs` | 完成 | 可修改 |
| `StatisticsConfig` | `Core/Models/StatisticsConfig.cs` | 完成 | 可修改 |
| `StatisticsResultItem` | `Core/Models/StatisticsResultItem.cs` | 完成 | 可修改 |
| `ChannelDisplayConfig` | `Core/Models/ChannelDisplayConfig.cs` | 完成 | 仅追加属性 |
| `ComputedChannelDefinition` | `Core/Models/ComputedChannelDefinition.cs` | 完成 | 仅追加属性 |
| `SessionInfo` | `Core/Storage/IDataStorageService.cs` | 完成 | 仅追加属性 |

---

## 五、DI 注册规范（`App/App.xaml.cs`）

当前注册：

```csharp
services.AddSingleton<IConnectionFactory, ConnectionFactory>();
services.AddSingleton<DataBus>();
services.AddTransient<ConnectionViewModel>();
services.AddTransient<RealtimeChartViewModel>();
services.AddTransient<MainViewModel>();
```

**各工作流追加注册（使用注释区域隔开）：**

```csharp
// ---- Stream A: 数据存储 (TASK-D1, D2, D4) ----
services.AddSingleton<IDataStorageService, SqliteStorageService>();
services.AddTransient<IDataExporter, CsvExporter>();
services.AddTransient<SessionListViewModel>();

// ---- Stream B: 历史回放 (TASK-D3) ----
services.AddTransient<HistoryPlaybackViewModel>();

// ---- Stream C: 校正 (TASK-C1~C5) ----
services.AddSingleton<IOrthogonalityService, OrthogonalityCalculator>();
services.AddSingleton<ICalibrationService, HardIronCalibration>();
services.AddSingleton<ICalibrationRepository, SqliteCalibrationRepository>();
services.AddTransient<OrthogonalityCalibrationViewModel>();

// ---- Stream E: 系统框架 (TASK-F2, F3) ----
services.AddSingleton<IAppConfigService, AppConfigService>();
```

---

## 六、MainViewModel 导航扩展规范

当前结构（`App/ViewModels/MainViewModel.cs`）：

```csharp
public ConnectionViewModel ConnectionVM { get; }
public RealtimeChartViewModel RealtimeChartVM { get; }
```

**各工作流追加的属性和命令：**

| 工作流 | 属性 | 导航命令 | 构造函数注入 |
|--------|------|---------|-------------|
| Stream A | `SessionListViewModel SessionListVM` | `NavigateToSessionList()` | `SessionListViewModel sessionListVm` |
| Stream B | `HistoryPlaybackViewModel HistoryPlaybackVM` | `NavigateToHistoryPlayback()` | `HistoryPlaybackViewModel historyPlaybackVm` |
| Stream C | `OrthogonalityCalibrationViewModel OrthoCalibVM` | `NavigateToOrthogonalityCalibration()` | `OrthogonalityCalibrationViewModel orthoCalibVm` |

导航命令模式：

```csharp
[RelayCommand]
private void NavigateToXxx()
{
    CurrentView = XxxVM;
}
```

---

## 七、并行工作流分配

```
Stream A (Agent 1) ─ 数据存储
  ├── TASK-D1: SQLite 数据库服务
  ├── TASK-D2: 会话管理
  └── TASK-D4: CSV 数据导出

Stream B (Agent 2) ─ 历史回放
  └── TASK-D3: 历史数据回放（依赖 D-1, D-2）

Stream C (Agent 3) ─ 校正与处理
  ├── TASK-C1: 正交度计算引擎
  ├── TASK-C2: 正交度校正应用（依赖 C-1）
  ├── TASK-C3: 正交度校正向导（依赖 C-1, C-2, D-1）
  ├── TASK-C4: 校正可视化（依赖 C-1, C-2）
  ├── TASK-C5: 校正配置管理（依赖 D-1）
  ├── TASK-E1: 实时统计引擎
  ├── TASK-E2: 数字滤波
  └── TASK-E3: 梯度计算

Stream D (Agent 4) ─ 图表增强
  ├── TASK-B3: LTTB 降采样
  └── TASK-B7: 滚动统计 UI 集成（依赖 E-1）

Stream E (Agent 5) ─ 系统框架
  ├── TASK-F2: 全局错误处理与日志
  └── TASK-F3: 配置持久化（依赖 D-1）
```

### 文件冲突矩阵

| 文件 | A | B | C | D | E |
|------|---|---|---|---|---|
| `App.xaml.cs` (DI) | 追加 | — | 追加 | — | 追加 |
| `MainViewModel.cs` | 追加 | 追加 | 追加 | — | — |
| `DataBus.cs` | 修改 | 读取 | 读取 | 读取 | — |
| `RealtimeChartViewModel.cs` | — | — | — | 修改 | — |
| `ConnectionViewModel.cs` | — | — | 修改(C-2) | — | — |

**冲突解决规则：**
1. `App.xaml.cs`: 各 Stream 使用注释区域追加，不修改现有行
2. `MainViewModel.cs`: 各 Stream 追加属性和命令，不修改现有代码
3. `DataBus.cs`: 仅 Stream A 添加 `SessionStarted/SessionEnded` 事件
4. 共享文件修改必须在独立方法中，不修改现有方法体

---

## 八、数据库 Schema（参考）

详见 `00-项目计划与任务书.md` 第三章。TASK-D1 按此 Schema 建表。

---

## 九、NuGet 依赖

| 包名 | 项目 | 用途 | 已安装 |
|------|------|------|--------|
| CommunityToolkit.Mvvm | App | MVVM 框架 | 是 |
| ScottPlot.WPF | App | 图表 | 是 |
| System.IO.Ports | Core | 串口 | 是 |
| MathNet.Numerics | Core | 矩阵运算/SVD/FFT | 是 |
| Dapper | Infrastructure | SQLite ORM | **待安装 (TASK-D1)** |
| Microsoft.Data.Sqlite | Infrastructure | SQLite 驱动 | **待安装 (TASK-D1)** |
| Serilog + Serilog.Sinks.File | App | 日志 | **待安装 (TASK-F2)** |
| xUnit + Moq | Tests | 测试 | **待安装 (TASK-TESTS)** |

---

*本文档随开发推进持续更新。各任务文档中的接口定义以本文档为准。*
