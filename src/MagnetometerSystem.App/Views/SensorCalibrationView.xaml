<UserControl x:Class="MagnetometerSystem.App.Views.SensorCalibrationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="900">
    <Grid Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- 左侧：配置列表 -->
        <DockPanel Grid.Column="0" Margin="0,0,10,0">
            <TextBlock DockPanel.Dock="Top" Text="校准配置列表" FontWeight="Bold" FontSize="14" Margin="0,0,0,8"/>
            <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,8">
                <Button Content="新建" Command="{Binding NewProfileCommand}" Padding="8,4" Margin="0,0,5,0"/>
                <Button Content="刷新" Command="{Binding LoadProfilesCommand}" Padding="8,4" Margin="0,0,5,0"/>
                <Button Content="删除" Command="{Binding DeleteProfileCommand}" Padding="8,4"/>
            </StackPanel>
            <ListBox ItemsSource="{Binding Profiles}" SelectedItem="{Binding SelectedProfile}"
                     DisplayMemberPath="Name"
                     SelectionChanged="OnProfileSelectionChanged"/>
        </DockPanel>

        <!-- 右侧：编辑区域 -->
        <ScrollViewer Grid.Column="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <TextBlock Text="校准参数编辑" FontWeight="Bold" FontSize="14" Margin="0,0,0,12"/>

                <TextBlock Text="配置名称" Margin="0,0,0,4"/>
                <TextBox Text="{Binding EditName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

                <TextBlock Text="传感器类型" Margin="0,0,0,4"/>
                <ComboBox ItemsSource="{Binding SensorTypes}" SelectedItem="{Binding EditSensorType}" Margin="0,0,0,10"/>

                <TextBlock Text="传感器序列号" Margin="0,0,0,4"/>
                <TextBox Text="{Binding EditSensorSerial, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

                <GroupBox Header="偏移量 (Offset)" Margin="0,0,0,10" Padding="8">
                    <StackPanel>
                        <TextBlock Text="各通道偏移量（逗号分隔，如 100.5, -50.2, 0）" Margin="0,0,0,4" FontSize="11" Foreground="Gray"/>
                        <TextBox Text="{Binding EditOffsetValues, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                </GroupBox>

                <GroupBox Header="增益系数 (Gain)" Margin="0,0,0,10" Padding="8">
                    <StackPanel>
                        <TextBlock Text="各通道增益系数（逗号分隔，如 1.002, 0.998, 1.001）" Margin="0,0,0,4" FontSize="11" Foreground="Gray"/>
                        <TextBox Text="{Binding EditGainValues, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                </GroupBox>

                <TextBlock Text="备注" Margin="0,0,0,4"/>
                <TextBox Text="{Binding EditNotes, UpdateSourceTrigger=PropertyChanged}"
                         AcceptsReturn="True" Height="60" TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto" Margin="0,0,0,12"/>

                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <Button Content="保存" Command="{Binding SaveProfileCommand}" Padding="15,6" Margin="0,0,10,0"/>
                </StackPanel>

                <TextBlock Text="{Binding StatusMessage}" FontStyle="Italic">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Foreground" Value="DarkGreen"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsStatusError}" Value="True">
                                    <Setter Property="Foreground" Value="Red"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
