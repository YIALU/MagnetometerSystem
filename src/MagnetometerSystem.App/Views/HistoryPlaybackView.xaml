<UserControl x:Class="MagnetometerSystem.App.Views.HistoryPlaybackView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
    </UserControl.Resources>

    <DockPanel Margin="10">
        <!-- 顶部: 会话选择 + 状态 -->
        <StackPanel DockPanel.Dock="Top" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="选择会话:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <ComboBox ItemsSource="{Binding AvailableSessions}"
                          SelectedItem="{Binding SelectedSession}"
                          DisplayMemberPath="Name" Width="300" Margin="0,0,10,0"/>
                <Button Content="加载数据" Command="{Binding LoadSessionCommand}" Padding="10,3" Margin="0,0,10,0"/>
                <Button Content="刷新列表" Command="{Binding RefreshSessionsCommand}" Padding="10,3" Margin="0,0,20,0"/>
                <TextBlock VerticalAlignment="Center">
                    <Run Text="状态: "/>
                    <Run Text="{Binding State, Mode=OneWay}" FontWeight="Bold"/>
                </TextBlock>
            </StackPanel>
        </StackPanel>

        <!-- 会话信息区 -->
        <Border DockPanel.Dock="Top" Background="#F5F5F5" Padding="10,6" Margin="0,0,0,10" CornerRadius="3">
            <Border.Style>
                <Style TargetType="Border">
                    <Setter Property="Visibility" Value="Visible"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding SelectedSession}" Value="{x:Null}">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Border.Style>
            <StackPanel Orientation="Horizontal">
                <TextBlock Margin="0,0,20,0">
                    <Run Text="传感器: " FontWeight="SemiBold"/>
                    <Run Text="{Binding SelectedSession.SensorType, Mode=OneWay}"/>
                </TextBlock>
                <TextBlock Margin="0,0,20,0">
                    <Run Text="采样率: " FontWeight="SemiBold"/>
                    <Run Text="{Binding SelectedSession.SampleRate, Mode=OneWay, StringFormat={}{0} Hz}"/>
                </TextBlock>
                <TextBlock Margin="0,0,20,0">
                    <Run Text="通道数: " FontWeight="SemiBold"/>
                    <Run Text="{Binding SelectedSession.ChannelCount, Mode=OneWay}"/>
                </TextBlock>
                <TextBlock Margin="0,0,20,0">
                    <Run Text="数据量: " FontWeight="SemiBold"/>
                    <Run Text="{Binding TotalReadings, Mode=OneWay, StringFormat=N0}"/>
                    <Run Text=" 条"/>
                </TextBlock>
                <TextBlock>
                    <Run Text="时长: " FontWeight="SemiBold"/>
                    <Run Text="{Binding TotalDuration, Mode=OneWay}"/>
                </TextBlock>
            </StackPanel>
        </Border>

        <!-- 加载中指示 -->
        <Border DockPanel.Dock="Top"
                Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}"
                Background="#E3F2FD" Padding="8,4" Margin="0,0,0,10" CornerRadius="3">
            <TextBlock Text="正在加载数据，请稍候..." FontWeight="Bold"/>
        </Border>

        <!-- 底部: 控制栏 -->
        <StackPanel DockPanel.Dock="Bottom" Margin="0,10,0,0">
            <!-- 进度条 -->
            <DockPanel Margin="0,0,0,5">
                <TextBlock DockPanel.Dock="Left" Text="{Binding ElapsedTime}" Width="80" VerticalAlignment="Center"/>
                <TextBlock DockPanel.Dock="Right" Text="{Binding TotalDuration}" Width="80"
                           TextAlignment="Right" VerticalAlignment="Center"/>
                <Slider Minimum="0" Maximum="1" Value="{Binding Progress}"
                        Thumb.DragStarted="OnSeekDragStarted"
                        Thumb.DragCompleted="OnSeekDragCompleted"
                        SmallChange="0.001" LargeChange="0.01"/>
            </DockPanel>

            <!-- 控制按钮 + 速度选择 -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Content="播放" Command="{Binding PlayCommand}" Width="60" Margin="5" Padding="3"/>
                <Button Content="暂停" Command="{Binding PauseCommand}" Width="60" Margin="5" Padding="3"/>
                <Button Content="停止" Command="{Binding StopCommand}" Width="60" Margin="5" Padding="3"/>
                <TextBlock Text="速度:" VerticalAlignment="Center" Margin="20,0,5,0"/>
                <ComboBox ItemsSource="{Binding AvailableSpeeds}"
                          SelectedItem="{Binding PlaybackSpeed}" Width="80">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding StringFormat={}{0}x}"/>
                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
                <TextBlock VerticalAlignment="Center" Margin="20,0,0,0">
                    <Run Text="{Binding CurrentIndex, Mode=OneWay, StringFormat=N0}"/>
                    <Run Text=" / "/>
                    <Run Text="{Binding TotalReadings, Mode=OneWay, StringFormat=N0}"/>
                </TextBlock>
            </StackPanel>
        </StackPanel>

        <!-- 中央: 提示区域（图表由 DataBus 驱动 RealtimeChartView 自动响应） -->
        <Border Background="#FAFAFA" CornerRadius="3" Margin="0,5">
            <TextBlock Text="回放数据将通过 DataBus 自动驱动实时图表显示。&#x0a;请在播放前确认已切换到实时采集页面查看图表，或在回放后查看。"
                       HorizontalAlignment="Center" VerticalAlignment="Center"
                       TextAlignment="Center" Foreground="#888" FontSize="14"/>
        </Border>
    </DockPanel>
</UserControl>
