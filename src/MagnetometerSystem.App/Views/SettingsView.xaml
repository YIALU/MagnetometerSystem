<UserControl x:Class="MagnetometerSystem.App.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto" Margin="15">
        <StackPanel MaxWidth="600">
            <TextBlock Text="系统设置" FontWeight="Bold" FontSize="18" Margin="0,0,0,20"/>

            <!-- 连接默认设置 -->
            <GroupBox Header="连接默认设置" Margin="0,0,0,12" Padding="10">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="120"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="默认串口:" VerticalAlignment="Center" Margin="0,0,0,8"/>
                    <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding DefaultPortName, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="默认波特率:" VerticalAlignment="Center" Margin="0,0,0,8"/>
                    <ComboBox Grid.Row="1" Grid.Column="1" ItemsSource="{Binding BaudRates}" SelectedItem="{Binding DefaultBaudRate}" Margin="0,0,0,8"/>

                    <TextBlock Grid.Row="2" Grid.Column="0" Text="默认 IP:" VerticalAlignment="Center" Margin="0,0,0,8"/>
                    <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding DefaultIpAddress, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>

                    <TextBlock Grid.Row="3" Grid.Column="0" Text="默认端口:" VerticalAlignment="Center" Margin="0,0,0,8"/>
                    <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding DefaultPort, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>
                </Grid>
            </GroupBox>

            <!-- 存储设置 -->
            <GroupBox Header="数据存储" Margin="0,0,0,12" Padding="10">
                <StackPanel>
                    <TextBlock Text="数据存储路径（留空使用默认路径）:" Margin="0,0,0,4"/>
                    <TextBox Text="{Binding DataStoragePath, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,8"/>
                    <CheckBox Content="自动保存采集数据" IsChecked="{Binding AutoSaveEnabled}" Margin="0,0,0,4"/>
                </StackPanel>
            </GroupBox>

            <!-- 图表设置 -->
            <GroupBox Header="图表显示" Margin="0,0,0,12" Padding="10">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="刷新率 (Hz):" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <ComboBox ItemsSource="{Binding RefreshRateOptions}" SelectedItem="{Binding ChartRefreshRate}" Width="80"/>
                </StackPanel>
            </GroupBox>

            <!-- UI 设置 -->
            <GroupBox Header="界面" Margin="0,0,0,12" Padding="10">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="主题:" VerticalAlignment="Center" Margin="0,0,8,0"/>
                    <ComboBox ItemsSource="{Binding ThemeOptions}" SelectedItem="{Binding ThemeName}" Width="120"/>
                </StackPanel>
            </GroupBox>

            <!-- 操作按钮 -->
            <StackPanel Orientation="Horizontal" Margin="0,10,0,10">
                <Button Content="保存设置" Command="{Binding SaveSettingsCommand}" Padding="15,6" Margin="0,0,10,0"/>
                <Button Content="恢复默认" Command="{Binding ResetToDefaultsCommand}" Padding="15,6" Margin="0,0,10,0"/>
                <Button Content="重新加载" Command="{Binding LoadSettingsCommand}" Padding="15,6"/>
            </StackPanel>

            <TextBlock Text="{Binding StatusMessage}" FontStyle="Italic" Margin="0,5,0,0">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Foreground" Value="DarkGreen"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsStatusError}" Value="True">
                                <Setter Property="Foreground" Value="Red"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>
        </StackPanel>
    </ScrollViewer>
</UserControl>
