<UserControl x:Class="MagnetometerSystem.App.Views.CalibrationVisualizationControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:scottplot="clr-namespace:ScottPlot.WPF;assembly=ScottPlot.WPF"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="900">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- XY 投影 -->
        <GroupBox Header="XY 平面投影" Grid.Row="0" Grid.Column="0" Margin="4">
            <scottplot:WpfPlot x:Name="XYPlot"/>
        </GroupBox>

        <!-- XZ 投影 -->
        <GroupBox Header="XZ 平面投影" Grid.Row="0" Grid.Column="1" Margin="4">
            <scottplot:WpfPlot x:Name="XZPlot"/>
        </GroupBox>

        <!-- YZ 投影 -->
        <GroupBox Header="YZ 平面投影" Grid.Row="0" Grid.Column="2" Margin="4">
            <scottplot:WpfPlot x:Name="YZPlot"/>
        </GroupBox>

        <!-- 总场时间序列 -->
        <GroupBox Header="总场强度对比" Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Margin="4">
            <scottplot:WpfPlot x:Name="TotalFieldPlot"/>
        </GroupBox>

        <!-- 残差直方图 -->
        <GroupBox Header="残差分布" Grid.Row="1" Grid.Column="2" Margin="4">
            <scottplot:WpfPlot x:Name="ResidualPlot"/>
        </GroupBox>
    </Grid>
</UserControl>
